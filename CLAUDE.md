# Outora

אתר השכרת אוהלי גלמפינג בישראל.
עובדים על ענף `YOTAM` · rebrand מלא ובנייה מחדש של כל הממשק, לפי הפיגמה של יותם.

---

## 🎨 חוק ראשון: הדיזיין סיסטם

**מקור האמת:** `lib/design-tokens.ts` → `app/globals.css` → `tailwind.config.ts`

**אסור בהחלט:**
- לכתוב קוד צבע (`#D9C18A`, `bg-[#fff]`) ישירות בקומפוננטה
- לכתוב גודל פונט (`text-[18px]`, `font-size: 18px`) ישירות
- להוסיף צל, פינה או ריווח שלא מהסולם
- להשתמש בקומפוננטות ברירת מחדל של Tailwind/shadcn בלי להתאים לטוקנים

**חובה:**
- צבע → `bg-beige`, `text-textgray`, `border-stroke` וכו'
- טקסט → `text-h1`, `text-h2`, `text-h3`, `text-subtitle`, `text-body`, `text-button`, `text-tag`
- צל → `shadow-drop`
- פינות → `rounded-sm|md|lg`
- אנימציה רכה → `ease-smooth`

**צריך ערך שאין בסיסטם?** אל תמציא. תשאל את יותם, ואחרי שהוא מאשר · תוסיף אותו לשלושת הקבצים למעלה, לא רק במקום שבו השתמשת.

---

## 🎨 הפלטה

| שם | הקס | לשימוש |
|---|---|---|
| `white` | `#FFFFFF` | רקע ראשי |
| `offwhite` | `#FBFBFB` | רקע משני |
| `stroke` | `#E6E6E6` | קווים, מסגרות |
| `textgray` | `#666666` | טקסט משני |
| `black` | `#000000` | טקסט ראשי |
| `beige` | `#D9C18A` | **ראשי** · כפתורים |
| `beigedark` | `#C7AC71` | בז' ב-hover ⚠️ מוערך |
| `orange` | `#D9652C` | **משני** · תגיות בלבד |
| `error` | `#B3261E` | ⚠️ לא מהפיגמה |
| `success` | `#2F7A55` | ⚠️ לא מהפיגמה |

**אין כפתורים כתומים.** כתום הוא לתגיות ולהדגשה בלבד.

## ✍️ טיפוגרפיה

פונט: **Almoni Neue** (`public/fonts/*.woff2`, נטען ב-`app/fonts.ts`).

| מחלקה | גודל | משקל |
|---|---|---|
| `text-h1` | 48 | DemiBold 600 |
| `text-h2` | 28 | DemiBold 600 |
| `text-h3` | 22 | DemiBold 600 |
| `text-subtitle` | 20 | Regular 400 |
| `text-body` | 16 | Regular 400 |
| `text-button` | 16 | Medium 500 |
| `text-tag` | 14 | Medium 500 | ⚠️ הוגדל מ-12, בפיגמה רשום 12

---

## 🧩 קומפוננטות קיימות

לפני שאתה בונה קומפוננטה · **תבדוק אם היא כבר קיימת** ב-`components/ui/`:

`button` · `input` · `field` (עם error/success) · `search-field` · `result-row` ·
`badge` · `alert` · `toast` · `card` · `checkbox` · `label` · `dropdown-menu`

**הכפתור:** ברירת מחדל = בז' עם מילוי מלמטה למעלה ב-hover.
יש `loading` (spinner), `muted`, `disabled`, ו-`variant="link"` (שחור → אפור ב-hover).

**דף התצוגה:** `/design-system` · כל הקומפוננטות במקום אחד.
**כשמוסיפים קומפוננטה חדשה · מוסיפים אותה גם לשם.**

---

## 🔒 מה לא נוגעים בו

הצד השרתי נשאר כמו שהוא. לא לשנות בלי בקשה מפורשת:

- `app/api/` · הזמנות, תשלומים, זמינות, וואטסאפ
- `middleware.ts` · הרשאות ואבטחה
- `lib/` (חוץ מ-`design-tokens.ts`) · לוגיקה עסקית
- `supabase/schema.sql` · מסד הנתונים

---

## 💬 איך לדבר עם יותם

יותם מעצב, לא איש טכנולוגיה.

- עברית פשוטה, משפטים קצרים, הודעות קצרות
- בלי ז'רגון טכני
- להגיד מה נעשה ומה הלאה · לא איך זה עובד מבפנים
- לא להציף בשמות קבצים ופקודות

---

## ⚙️ הרצה

הפרויקט יושב ב-`C:\Outora`. **לא לעבוד על העותק ב-`G:`** · הדיסק שם איטי בצורה בלתי שמישה.

```
npm run dev      # http://localhost:3000
```

---

## ❓ פתוח · צריך אישור מיותם

- הקס מדויק לבז' ב-hover (כרגע `#C7AC71`)
- פינות מעוגלות (כרגע 8 / 12 / 16)
- צבעי error ו-success
- סולם ריווחים (כרגע 4px סטנדרטי)

---

## 🧭 ההדר

ההדר שקוף רק בדפים שמתחילים בתמונת רקע: דף הבית וכל תהליך ההזמנה (`/book`).
בכל שאר הדפים הרקע לבן מהרגע הראשון, אחרת הלוגו והתפריט הלבנים נעלמים על רקע לבן.

**דף חדש עם תמונת הירו?** להוסיף אותו ל-`HERO_ROUTES` או ל-`HERO_PREFIXES` ב-`components/layout/header.tsx`.

---

## 🚫 אסור בהחלט

**התו (em dash) אסור לשימוש. בשום מקום.**

לא בקוד, לא בתוכן, לא בהערות, לא בהודעות קומיט, לא בשיחה עם יותם.

במקומו:
- `·` כמפריד
- `:` להצגת הסבר
- `,` או `.` בתוך משפט
- `-` כמקף רגיל

התו `–` (en dash) מותר, הוא בשימוש בטווחי תאריכים ושעות.

---

## ↔️ כלל RTL — הטעות החוזרת ביותר

האתר כולו `dir="rtl"`. בשורת flex, **הילד הראשון בקוד מופיע הכי ימינה.**

אסור לכתוב את הילדים לפי איך שקוראים באנגלית. **לכתוב אותם מימין לשמאל, כפי שהם ייראו במסך.**

```jsx
// רוצים: כותרת מימין, מחיר משמאל
<div className="flex justify-between">
  <h3>כותרת</h3>   {/* ראשון = ימין ✓ */}
  <p>מחיר</p>        {/* שני   = שמאל ✓ */}
</div>
```

**לפני שמוסרים מסך:** לעבור על כל `flex` בקובץ ולוודא שסדר הילדים תואם לפיגמה.

גם:
- `justify-end` ב-RTL דוחף **שמאלה**, לא ימינה.
- `w-fit` על אלמנט בלוק מצמיד אותו **לימין**.
- למרווחים ומיקום להשתמש ב-`ms-` / `me-` / `start-` / `end-`, לא ב-`ml-` / `mr-` / `left-` / `right-`.

---

## 📋 מסמך לאוטורה

בשורש הפרויקט יש `NOTES-FOR-OUTORA.md` — רשימה חיה של פערים במוצר, סתירות בנתונים ושאלות שצריכות תשובה מהלקוח.

**כל פעם שמתגלה משהו כזה — להוסיף אותו לשם מיד**, ולעדכן את תאריך העדכון בראש הקובץ. לא לחכות שיבקשו.
