# SETUP · איפה כל דבר מנוהל

המסמך הזה עונה על שאלה אחת: **"איפה נכנסים כדי לשנות את X?"**

**עודכן:** 25.08.2026

> **הסיסמאות עצמן לא נמצאות כאן.** הקובץ הזה נשמר ב-GitHub, וכל מי שיש לו גישה לקוד רואה אותו.
> הסיסמאות והמפתחות האמיתיים יושבים ב-`SECRETS.local.md` · קובץ שנשאר רק על המחשב ולא עולה לשום מקום.
> כאן כתוב **איפה** כל דבר ו**מי** בעל החשבון. שם כתוב **מה** הערך.

---

## 🗺️ מפה מהירה

| מה | איפה מנוהל | בעל החשבון | לינק |
|---|---|---|---|
| הדומיין `outora.co.il` | **box.co.il** | ❓ לברר | https://box.co.il |
| שרתי DNS (רשומות) | **Vercel** ✅ הועבר 24.08.2026 | רז | https://vercel.com |
| האתר עצמו · העלאות לאוויר | **Vercel** · פרויקט `outora-web` | רז | https://vercel.com |
| הקוד | **GitHub** · `Raz-Projects/outora` | רז | https://github.com/Raz-Projects/outora |
| מסד נתונים · הזמנות, לקוחות, תוכן | **Supabase** · `nhatapbytpujtgzkgalp` | רז | https://supabase.com/dashboard |
| תשלומים | **Grow** (מיטב) | ❓ לברר | https://grow.business |
| מיילים שהמערכת שולחת | **Resend** | ❓ לברר | https://resend.com |
| וואטסאפ | **Meta** · WhatsApp Business | ❓ לברר | https://business.facebook.com |
| תיבות מייל של הצוות | ❌ עדיין לא קיים | · | ראה [מייל ממותג](#-מייל-ממותג-outoracoil) |

---

## 🌐 הדומיין · box.co.il

**מה מנהלים שם:** את הבעלות על הדומיין בלבד. חידוש שנתי, פרטי בעלים, ולאיזה שרתי DNS הוא מצביע.

**מה לא מנהלים שם:** את הרשומות עצמן. אלה עברו לוורסל.

- **תוקף:** עד 31.12.2027
- **כניסה:** box.co.il → אזור אישי → ניהול דומיינים → outora.co.il → "עדכן דומיין"
- **שתי לשוניות:**
  - `עורך אזור DNS` · **לא משתמשים בזה יותר**
  - `עורך NS` · כאן מצביעים על וורסל

### שרתי ה-NS הנכונים

```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

שורה 3 והלאה נשארות **ריקות**. אסור להשאיר שרת של box ביחד עם וורסל.

> ⚠️ box כותב שעדכון NS לוקח **עד 24 שעות**. בפועל בדרך כלל כמה שעות.

---

## ▲ האתר · Vercel

**מה מנהלים שם:** את האתר החי, את הכתובות שלו, את המשתנים הסודיים, ומעכשיו גם את רשומות ה-DNS.

- **פרויקט:** `outora-web`
- **מחובר ל:** GitHub · `Raz-Projects/outora`

### איך עולה גרסה חדשה לאוויר

אוטומטי. כל דחיפה של קוד לענף הראשי ב-GitHub מעלה גרסה חדשה תוך כמה דקות.
ענפים אחרים (כמו `YOTAM`) מקבלים כתובת תצוגה נפרדת, ולא נוגעים באתר החי.

### הכתובות

| כתובת | תפקיד | סטטוס |
|---|---|---|
| `outora.co.il` | **הכתובת הראשית** · הוחלט 24.08.2026 | ✅ |
| `www.outora.co.il` | מפנה אוטומטית לכתובת הקצרה (308) | ✅ |
| `admin.outora.co.il` | ממשק הניהול | ✅ |
| `outora-web.vercel.app` | כתובת ברירת המחדל של וורסל | ✅ |

> ⚠️ **הכיוון מוגדר בשני מקומות, והם חייבים להסכים:**
> בוורסל (Domains → Edit) **וגם** בקוד ב-`next.config.ts`, בכלל שמפנה `www` לכתובת הקצרה.
> אם שניהם מפנים אחד לשני, האתר נכנס ללולאה אינסופית ולא נטען בכלל. קרה לנו ב-24.08.2026.

### איך מוסיפים כתובת חדשה

Vercel → הפרויקט → Settings → Domains → **Add Existing** → מקלידים את הכתובת → Add.

כל עוד ה-NS מצביעים על וורסל, אין מה לעשות ב-box. וורסל מייצר את הרשומה לבד.

### המפתחות · Environment Variables

Vercel → הפרויקט → Settings → **Environment Variables**.
אחרי כל שינוי צריך **Redeploy**, אחרת הוא לא נכנס לתוקף.

**12 המשתנים שחייבים להיות שם:**

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
SUPABASE_SECRET_KEY
NEXT_PUBLIC_SITE_URL
GROW_API_KEY
GROW_MERCHANT_ID
GROW_WEBHOOK_SECRET
RESEND_API_KEY
EMAIL_FROM
EMAIL_TEAM
WHATSAPP_PHONE_ID
WHATSAPP_TOKEN
WHATSAPP_VERIFY_TOKEN
```

הערכים יושבים ב-`.env.local` על המחשב. אפשר להעתיק את כל הקובץ ולהדביק בבת אחת
בתיבת ההדבקה של וורסל.

⚠️ **חוץ מאחד:** `NEXT_PUBLIC_SITE_URL` הוא `http://localhost:3000` מקומית,
ובוורסל הוא חייב להיות `https://outora.co.il`. אחרת הקישורים במיילים ובאישורי
ההזמנה יובילו לכתובת הלא נכונה.

> **איך יודעים שהם חסרים:** האתר השיווקי ייראה תקין (הדפים סטטיים),
> אבל `/admin` יחזיר שגיאה 500, כניסה עם מייל לא תעבוד, והזמנות לא יישמרו.
> קרה ב-24.08.2026.

### ⚠️ המלכודת · Sensitive מול Config

בוורסל יש כפתור `Sensitive` בהוספת משתנה. הוא נראה כמו "יותר בטוח", אבל:

**משתנה שמתחיל ב-`NEXT_PUBLIC_` חייב להיות `Config`, לא `Sensitive`.**

וורסל מסתיר משתני `Sensitive` מהקוד שרץ בדפדפן, וזו המטרה שלהם. אבל שלושת
המשתנים האלה **אמורים** להיות גלויים לדפדפן, אחרת האתר לא יודע לאן להתחבר:

| משתנה | הסימון הנכון |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Config |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Config |
| `NEXT_PUBLIC_SITE_URL` | Config |
| **כל השאר (10)** | **Sensitive** |

הסימן לתקלה: המשתנה קיים ברשימה, נראה תקין, ובכל זאת האתר מתנהג כאילו אין מסד נתונים.

> לא ניתן לבטל `Sensitive` בעריכה. חייבים למחוק את המשתנה ולהוסיף אותו מחדש.
> קרה ב-24.08.2026, ולקח זמן לאתר.

---

## 📇 רשומות DNS

המעבר הושלם ב-24.08.2026. וורסל מייצר את הרשומות של האתר לבד, כולל רשומת `*` שמכסה כל תת-דומיין. אין מה לגעת בהן.

**רשומות שכן נצטרך להוסיף ידנית בעתיד** (Vercel → Domains → outora.co.il → Vercel DNS):

| מתי | סוג | בשביל מה |
|---|---|---|
| כשמוסיפים תיבות מייל | `MX` | לקבל דואר ל-`@outora.co.il` |
| כשמחברים את Resend | `TXT` + `CNAME` | שהמיילים של המערכת לא ייפלו לספאם |
| כשמאמתים Google / Meta | `TXT` | הוכחת בעלות על הדומיין |

### מצב הרשומות לפני המעבר (לתיעוד)

```
A      @      216.198.79.1                              ← box לא הצליח לפרסם אותה
CNAME  www    6ae37cf3c088c670.vercel-dns-017.com       ← עבד
```

זו בדיוק הסיבה למעבר לוורסל.

---

## 🗄️ מסד הנתונים · Supabase

**מה יושב שם:** הזמנות, לקוחות, טיוטות הזמנה, קודי הנחה, המלאי, ותוכן האתר (מיקומים, חבילות, אוהלים, תוספות).

- **מזהה הפרויקט:** `nhatapbytpujtgzkgalp`
- **כתובת:** `https://nhatapbytpujtgzkgalp.supabase.co`
- **לוח בקרה:** https://supabase.com/dashboard/project/nhatapbytpujtgzkgalp

### שני סוגי מפתחות · חשוב להבין את ההבדל

| מפתח | רגישות | איפה |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | 🟢 פומבי, לא נורא אם נחשף | נמצא ממילא בקוד של הדפדפן |
| `SUPABASE_SECRET_KEY` | 🔴 **סודי מאוד** · עוקף כל הרשאה | רק ב-Vercel וב-`.env.local` |

> ❌ **לעולם לא לשלוח את המפתח הסודי בוואטסאפ, במייל או בצ'אט.**
> אם נחשף בטעות: Supabase → Settings → API → Rotate.

### שינויים במבנה הנתונים

הכל דרך קבצים ב-`supabase/migrations/`, לפי מספור. הקובץ האחרון כרגע: `013_fix_draft_upsert.sql`.
לא לשנות טבלאות ידנית בלוח הבקרה, אחרת הקוד והמסד יוצאים מסנכרון.

---

## 💳 תשלומים · Grow

סליקה דרך Grow (מיטב). המערכת מדברת איתם בשלושה מפתחות:

| משתנה | בשביל מה |
|---|---|
| `GROW_MERCHANT_ID` | מזהה בית העסק |
| `GROW_API_KEY` | 🔴 סודי |
| `GROW_WEBHOOK_SECRET` | 🔴 סודי · מאמת שהודעת "שולם" באמת הגיעה מ-Grow |

**כתובת ה-webhook שצריכה להיות מוגדרת אצל Grow:**
```
https://outora.co.il/api/payment/webhook
```
⚠️ **אחרי שהדומיין עולה, חובה לעדכן אותה אצל Grow.** אחרת תשלומים לא ייקלטו.

---

## ✉️ מיילים שהמערכת שולחת · Resend

אישורי הזמנה, קודי כניסה, התראות לצוות. זה **לא** תיבת דואר, זו רק שליחה אוטומטית.

| משתנה | בשביל מה |
|---|---|
| `RESEND_API_KEY` | 🔴 סודי |
| `EMAIL_FROM` | הכתובת שממנה יוצא הדואר · ❓ עדיין ריק |
| `EMAIL_TEAM` | לאן מגיעות התראות על הזמנה חדשה · ❓ עדיין ריק |

**כדי ש-Resend ישלח בשם `@outora.co.il`:**
Resend → Domains → Add Domain → `outora.co.il`. הוא ייתן שלוש רשומות, ומדביקים אותן ב-Vercel DNS.
בלי זה, המיילים ייפלו לספאם.

---

## 💬 וואטסאפ · Meta

| משתנה | בשביל מה |
|---|---|
| `WHATSAPP_PHONE_ID` | מזהה מספר הטלפון העסקי |
| `WHATSAPP_TOKEN` | 🔴 סודי |
| `WHATSAPP_VERIFY_TOKEN` | 🔴 סודי · מאמת הודעות נכנסות |

**כתובת ה-webhook:** `https://outora.co.il/api/whatsapp/webhook`
⚠️ גם אותה צריך לעדכן אצל Meta אחרי שהדומיין עולה.

---

## 🔑 מסך הסיסמה של התצוגה המוקדמת

האתר סגור מאחורי מסך "בקרוב" עד ההשקה.

- **הסיסמה:** `razi`
- **איפה משנים:** `components/layout/preview-gate.config.ts`
- **ביום ההשקה:** לשנות `PREVIEW_ENABLED = true` ל-`false`, ולהעלות לאוויר

> ⚠️ הבדיקה נעשית בדפדפן, אז הסיסמה גלויה למי שיודע להסתכל בקוד המקור.
> היא מרחיקה סקרנים, היא לא הגנה אמיתית. ממשק הניהול לא מסתמך עליה.

---

## 👤 מי נכנס לממשק הניהול

הניהול נעול מאחורי כניסה עם קוד למייל, ורק המיילים ברשימה הזאת מורשים:

- `raz@outora.co.il`
- `arad@outora.co.il`
- `razaror96@gmail.com`
- `yotamh@edenmedia.co.il`

**איפה משנים:** `lib/admin/config.ts`
כל שינוי דורש העלאה לאוויר.

**אורך קוד הכניסה** נקבע ב-Supabase (Authentication → Emails → OTP length), לא בקוד.
כרגע הוא **8 ספרות**. טופס הכניסה לא בודק אורך בכלל, אז שינוי ההגדרה שם לא ישבור אותו.

> ⚠️ שתי הכתובות ב-`@outora.co.il` עדיין **לא באמת קיימות** · אין תיבות דואר על הדומיין.
> עד שיוקמו, אי אפשר לקבל אליהן את קוד הכניסה.

---

## 📮 מייל ממותג `@outora.co.il`

כרגע אין שום תיבת דואר על הדומיין. בדקנו, אין רשומות MX.

חשוב להבין שיש שני דברים נפרדים, וצריך את שניהם:

| | מה זה | דרך מי |
|---|---|---|
| **תיבת דואר** | מקום שרז ואראד קוראים ושולחים ממנו מייל | Google Workspace / Zoho |
| **שליחה אוטומטית** | אישורי הזמנה שהמערכת שולחת ללקוחות | Resend (כבר בקוד) |

### מה מומלץ

**Google Workspace** · בערך 25 ש"ח לחודש למשתמש. זה בדיוק Gmail, רק עם הכתובת שלכם. הכי מוכר ופשוט.

**Zoho Mail** · יש מסלול חינם עד 5 משתמשים. חוסך כסף, פחות נוח.

### איך מקימים

1. נרשמים ל-Google Workspace ומקלידים את `outora.co.il` כדומיין.
2. גוגל מבקש להוכיח בעלות ונותן רשומות DNS.
3. מדביקים אותן ב-**Vercel** → Domains → outora.co.il → Vercel DNS.
4. פותחים תיבות: `raz@`, `arad@`, `info@`.
5. חוזרים ל-Resend ומחברים גם אותו לדומיין, כדי שהמיילים האוטומטיים יצאו מהכתובת הנכונה.

> **סדר הפעולות:** קודם מסיימים את מעבר ה-NS לוורסל, רק אחר כך פותחים מייל.
> אחרת הרשומות ייכתבו במקום הלא נכון ויימחקו במעבר.

---

## 🧭 מדריכים קצרים

### רוצה לשנות תוכן באתר (מיקום, חבילה, מחיר)
`admin.outora.co.il` → תוכן. אין צורך במפתח או במפתח סודי.

### רוצה להוסיף כתובת חדשה לאתר
Vercel → Domains → Add Existing.

### רוצה להוריד את מסך הסיסמה
`components/layout/preview-gate.config.ts` → `PREVIEW_ENABLED = false` → העלאה לאוויר.

### רוצה להוסיף מישהו לניהול
`lib/admin/config.ts` → להוסיף את המייל לרשימה → העלאה לאוויר.

### רוצה לבדוק אם הדומיין תפס
Vercel → Domains. ירוק = עובד. אדום = עוד לא.

### רוצה לראות הזמנות
`admin.outora.co.il` → הזמנות.

---

## ⏭️ מה עוד פתוח

- [x] ~~להשלים את מעבר ה-NS לוורסל~~
- [x] ~~להוסיף `admin.outora.co.il` בוורסל~~
- [ ] להקים תיבות מייל על הדומיין
- [ ] לחבר את Resend לדומיין ולמלא `EMAIL_FROM` ו-`EMAIL_TEAM`
- [ ] למלא את פרטי Grow ולעדכן אצלם את כתובת ה-webhook
- [ ] לעדכן ב-Meta את כתובת ה-webhook של וואטסאפ
- [x] ~~לעדכן `NEXT_PUBLIC_SITE_URL` בוורסל~~
- [ ] לברר מי בעל החשבון ב-box, ב-Grow, ב-Resend וב-Meta
- [ ] ביום ההשקה: להוריד את מסך הסיסמה

---

## 📝 יומן שינויים

| תאריך | מה קרה |
|---|---|
| 24.08.2026 | נוצר המסמך. הדומיין `outora.co.il` נרכש ב-box, ה-NS בתהליך מעבר לוורסל, נוסף ניתוב ל-`admin.outora.co.il` בקוד |
| 24.08.2026 | ה-NS עברו לוורסל ✅ · הוחלט ש-`outora.co.il` (בלי www) היא הכתובת הראשית · תוקנה לולאת הפניות בין השתיים |
| 24.08.2026 | ✅ **האתר חי על `outora.co.il`** · תעודת אבטחה תקינה, `www` ו-`http` מפנים נכון |
| 24.08.2026 | ענף `YOTAM` מוזג למיין והועלה לאוויר · האתר החדש והניהול חיים. התגלה ש-Vercel בנה בלי אף משתנה סביבה |
| 24.08.2026 | ✅ **הכל חי** · הניהול, הכניסה והמסד עובדים. הסיבה לתקלה: שלושת משתני `NEXT_PUBLIC_` היו מסומנים `Sensitive`, ווורסל חסם אותם מהדפדפן |
| 25.08.2026 | תוקן טופס הכניסה לניהול · הוא חסם קוד ארוך מ-6 ספרות, בזמן ש-Supabase שולח 8 |
